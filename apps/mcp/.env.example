# Required for both local HTTP and stdio modes.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required only for local stdio mode. Use a short-lived Supabase user access token.
MCP_DEV_ACCESS_TOKEN=
